name: "CI Planet"

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  linter:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run installation
      - run: yarn install
      - name: Run linter
      - run: yarn run lint
